package com.haxorz.ChronoTimer.Commands;

/**
 * enumeration of commands the system supports
 */
public enum CmdType {
    POWER,TOG,EXIT,RESET,TIME,DNF,CANCEL,TRIG,START,FINISH,EVENT, NEWRUN, ENDRUN, NUM, PRINT, DISC, CLR, SWAP, CONN, EXPORT, PRINTPWR
}
